using UnityEngine;
public enum Enemy_Types { Standard, Shark, Bulldozer, Medic, Officer, Ranger };
public enum Difficulty_Levels { Battalion, Regiment, Brigade, Division, Army };
public class Enum_Declaration_script : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}

// There should only ever be one of these scripts in a given game

